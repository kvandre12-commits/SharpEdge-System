# Configs

This folder holds non-authoritative routing and target configuration for Phone Companion.

Examples:
- domain -> default launcher
- domain -> default view
- channel -> package mapping
- browser/channel preferences

Config here does not grant authority.
It only steers delivery behavior.
